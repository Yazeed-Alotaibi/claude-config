# coralline theme: catppuccin-mocha
# Pastel pills with dark text; dark gauge segments with pastel bars
VL_BG_DIR="137,180,250"        # blue
VL_BG_GIT_OK="166,227,161"     # green
VL_BG_GIT_DIRTY="250,179,135"  # peach
VL_BG_MODEL="203,166,247"      # mauve
VL_BG_CTX="49,50,68"           # surface0
VL_BG_5H="36,36,52"
VL_BG_7D="24,24,37"            # crust
VL_BG_COST="245,194,231"       # pink
VL_BG_CLOCK="116,199,236"      # sapphire
VL_BG_LINES="49,50,68"
VL_BG_STYLE="242,205,205"      # flamingo
VL_BG_DURATION="148,226,213"   # teal
VL_BG_EFFORT="180,190,254"     # lavender

VL_FG_TEXT="30,30,46"          # base (dark text on pastel pills)
VL_FG_DIM="108,112,134"        # overlay0
VL_FG_OK="166,227,161"
VL_FG_WARN="249,226,175"
VL_FG_HOT="243,139,168"
VL_BG_NODE="166,227,161"
VL_BG_PYTHON="137,220,235"     # Sky #89dceb (Catppuccin accent)

# Subagent panel: subseg_name tints the label by task status out of the main
# VL_FG_* palette, which is tuned for the gauge segments' dark ground. Giving the
# name pill that same ground keeps every status legible in this theme's own light
# inks. Targets are WCAG AA for normal-size text (terminal type is not 'large'
# even in bold), checked against both this pill and the uniform bar
# VL_STYLE="classic" paints instead; running sits a step higher so it never
# collides with the dim tint. These are candidates, not the public knobs:
# statusline.sh adopts them only while _VL_SUB_FP still matches the live palette,
# so a config that retints VL_FG_* after sourcing this theme keeps its own colors.
_VL_SUB_BG_NAME="49,50,68"     # dark ground, same family as the gauge segments
_VL_SUB_FG_TEXT="176,205,252"  # running (7.77 pill / 6.02 classic bar)
_VL_SUB_FG_OK=""               # completed falls through at 8.46 / 6.55
_VL_SUB_FG_HOT="244,149,175"   # failed (5.85 / 4.53)
_VL_SUB_FG_DIM="173,176,189"   # unknown (5.82 / 4.51)
_VL_SUB_FP="137,180,250|30,30,46|166,227,161|243,139,168|108,112,134"
_VL_SUB_BAR="|"
